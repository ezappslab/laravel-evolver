{
    "name": "ezappslab/laravel-evolver",
    "description": "Laravel Evolver is a migration-like system for application evolution actions, designed to handle one-time data transformations, API migrations, or any logic that needs to execute once based on your application's version.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Radoslav Boydev"
        }
    ],
    "autoload": {
        "psr-4": {
            "Infinity\\Evolver\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Workbench\\App\\": "workbench/app/",
            "Workbench\\Database\\Factories\\": "workbench/database/factories/",
            "Workbench\\Database\\Seeders\\": "workbench/database/seeders/",
            "Tests\\": "tests/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Infinity\\Evolver\\EvolverServiceProvider"
            ]
        }
    },
    "require": {
        "php": "^8.2 || ^8.3 || ^8.4 || ^8.5",
        "illuminate/contracts": "^11.0 || ^12.0 || ^13.0",
        "spatie/laravel-package-tools": "^1.92"
    },
    "require-dev": {
        "orchestra/testbench": "^v10.9 || v11.0",
        "larastan/larastan": "^v3.8",
        "laravel/pint": "^v1.25",
        "pestphp/pest": "^v4.1",
        "pestphp/pest-plugin": "^v4.0",
        "pestphp/pest-plugin-arch": "^v4.0",
        "pestphp/pest-plugin-mutate": "^v4.0",
        "pestphp/pest-plugin-laravel": "^v4.0"
    },
    "scripts": {
        "post-autoload-dump": [
            "@clear",
            "@prepare"
        ],
        "clear": "@php vendor/bin/testbench package:purge-skeleton --ansi",
        "prepare": "@php vendor/bin/testbench package:discover --ansi",
        "build": "@php vendor/bin/testbench workbench:build --ansi",
        "serve": [
            "Composer\\Config::disableProcessTimeout",
            "@build",
            "@php vendor/bin/testbench serve --ansi"
        ],
        "lint": [
            "@php vendor/bin/pint --ansi",
            "@php vendor/bin/phpstan analyse --verbose --ansi"
        ],
        "test": "@php vendor/bin/pest"
    },
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    }
}
